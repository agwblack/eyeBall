#ifndef _TYPES_H_
#define _TYPES_H_

/* *
 * Cartesian measures a signed magnitude in both the horizontal and vertical
 * plane
 * */
typedef struct 
{
  double x;
  double y;
} Cartesian;

/* *
 * Holds all necessary information for physics models to work on entities
 * TODO: Implement!
 */
typedef struct
{
} PhysicalProperties;
#endif
